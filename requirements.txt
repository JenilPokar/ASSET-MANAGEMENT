# ============================================================================
# ASSET MANAGEMENT SYSTEM - REQUIREMENTS
# ============================================================================
# Install the Python libraries with:
#   pip install -r requirements.txt
#
# Everything below the libraries section is NOT pip-installable -- it's
# listed here as a reference for what else needs to be set up before the
# app will run. See 04_documentation_mysql_tkinter.md for full steps.
# ============================================================================

# ---------------------------------------------------------------------------
# PYTHON LIBRARIES (installed via pip)
# ---------------------------------------------------------------------------
mysql-connector-python>=8.0.33   # MySQL driver used by db_connector.py

# ---------------------------------------------------------------------------
# BUILT-IN PYTHON MODULES (no install needed, listed for completeness)
# ---------------------------------------------------------------------------
# tkinter    - GUI toolkit (ships with Python on Windows/macOS;
#              on Linux: sudo apt install python3-tk)
# csv        - report export in main_app.py
# hashlib    - SHA-256 password hashing to match MySQL's SHA2()
# datetime   - date handling in the GUI forms

# ---------------------------------------------------------------------------
# SYSTEM / SOFTWARE REQUIREMENTS (install separately, not via pip)
# ---------------------------------------------------------------------------
# Python           3.9 or newer
# MySQL Server     8.0.16 or newer (CHECK constraints need 8.0.16+)
# MySQL Workbench  latest (used to run the .sql files and inspect the schema)

# ---------------------------------------------------------------------------
# PROJECT FILES THIS APP DEPENDS ON (not libraries -- included in the deliverable)
# ---------------------------------------------------------------------------
# 01_mysql_schema_and_data.sql                 - run first, in MySQL Workbench
# 02_mysql_procedures_functions_triggers.sql   - run second, in MySQL Workbench
# db_connector.py                              - imported by main_app.py

#Flask
#gunicorn
